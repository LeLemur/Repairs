<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Awesome Repair Order System</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"/>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">Repair Order System</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mr-auto" id="navLinks">
        <li class="nav-item active">
          <a class="nav-link" href="#" data-tab="orders">Orders</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#" data-tab="customers">Customers</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#" data-tab="parts">Parts</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#" data-tab="reports">Reports</a>
        </li>
        <% if (user.role === 'admin') { %>
        <li class="nav-item">
          <a class="nav-link" href="#" data-tab="admin">Admin</a>
        </li>
        <% } %>
      </ul>
      <span class="navbar-text">
        Welcome, <%= user.username %>!
      </span>
      <ul class="navbar-nav ml-3">
        <li class="nav-item">
          <a class="nav-link" href="/logout">Logout</a>
        </li>
      </ul>
    </div>
  </nav>

  <!-- Main content area -->
  <div class="container mt-4" id="mainContent">
    <!-- Dynamic content will be injected here -->
  </div>

  <!-- Pass the user role to client-side -->
  <script>
    window.userRole = "<%= user.role %>";
  </script>
  <!-- jQuery, Popper, Bootstrap -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <!-- Our application script -->
  <script src="/script.js"></script>
</body>
</html>
